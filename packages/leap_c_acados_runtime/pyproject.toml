[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "leap-c-acados-runtime"
version = "0.5.1"
requires-python = ">=3.11"
description = "Pre-compiled C shared libraries and headers for acados"

[tool.setuptools.packages.find]
include = ["leap_c_acados_runtime*"]

[tool.setuptools.package-data]
leap_c_acados_runtime = ["lib/**/*", "include/**/*", "bin/**/*"]

[tool.cibuildwheel]
build = "cp311-* cp312-* cp313-*"
skip = "pp* *-musllinux*"
test-command = "python -c 'import leap_c_acados_runtime; print(\"leap_c_acados_runtime ok\")'"

[tool.cibuildwheel.linux]
before-build = "bash scripts/build_acados_c.sh"
